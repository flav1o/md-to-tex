import { Theme } from "@mui/material";
import { makeStyles } from "@mui/styles";

const LatexPreviewerStyles = makeStyles((theme: Theme) => ({
  container: {
    width: "100%",
  },
}));

export default LatexPreviewerStyles;
